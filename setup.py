#!/usr/bin/env python

from setuptools import setup

setup(name='hovercraft',
      version='0.1',
      description='Hovercraft Slides',
      author='Pygrunn',
      install_requires=[
        'flask',
        'flask_oauth',
        ],
     )
